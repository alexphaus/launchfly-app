// src/app/copilot2/login/page.tsx
// One account, three shells: the same sign-in, inside the four-tab layout.
import LoginView from '../../copilot/_components/LoginView';

export default function Copilot2LoginPage() {
  return <LoginView />;
}
