// src/app/copilot/login/page.tsx
// The bold sign-in. /lifeos/login renders the same view in the calm shell.
import LoginView from '../_components/LoginView';

export default function CopilotLoginPage() {
  return <LoginView />;
}
